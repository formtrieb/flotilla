## Convention 14 — evidence sidecar: live occurrences

Moved out of `reference/convention-14-citation-placement.md` per ADR-0034's Amendment (2026-08-13): `reference/` is loaded whole by every execution skill on every wave, and this history is not needed for that load. Reachable on demand via the ADR-0040 sibling-path read; the rule itself stays in the reference/ file.

### Live occurrences (evidence)

- **2026-07-30, the grill session behind ADR-0032.** The citation-placement question was raised alongside the engine-invocation decision and settled in the same session, then recorded in that ADR as a related editorial decision deliberately **not** ADR-material — reversible, no hard trade-off — "to land as a wave-shared convention with a reference-guard predicate and a shrinking legacy allowlist" (`docs/adr/0032-engine-invocation-is-a-setup-time-binding.md`). This file is that landing.
- **Seeding measurement, same date.** 146 citation occurrences across the 12 shipped SKILL.md bodies, of which 130 already sat in a provenance position and 16 did not — spread over six files (the load-bearing figure: it is what `CITATION_PLACEMENT_LEGACY`'s six seeded ceilings sum to, and it is what the allowlist ratchet actually protects). The convention codifies what the corpus was already doing nine times out of ten; the allowlist covers the remainder. **Derivation:** these are the counts a fresh run of the guard's own extraction logic (`extractCitations` over every `SKILL_BODIES` file in `tools/wave/src/skill-reference-guard.spec.ts`) produces against the current corpus — a total, a provenance-positioned count, and a narrative count, the last of which the population test (`MIN_BODY_CITATIONS`) and the ratchet test both key off directly. The 153/137 pair that first landed here counted something other than what the guard measures and is superseded by this line.

# flotilla — Retrospektive: Wave 27 „2026-07-23-w26-followups" (achtundzwanzigster Live-Lauf)

Wave: `2026-07-23-w26-followups` · Rows: **FOR-90 / FOR-91** (die beiden W26-Funde) · Store: Linear (Formtrieb/`FOR`) · Repo: `github.com/formtrieb/flotilla` (öffentlich) · Anchor: `9bbdd58` → `main` nach dem Close: `70af57b`.

Fünfte Wave des Tages, die schnellste bisher (~15 min Dispatch-bis-Verdict, 8 Agents, 0 Fehler) — und eine doppelte Premiere: **die erste Wave, deren Close beide offenen Live-Gates bestand**, und **die erste Wave ohne einen einzigen neuen Fund**. Ihre Fund-Tabelle ist leer; der Backlog dahinter auch.

## 0. Ergebnis in einem Satz

Wave 27 materialisierte das Duo (disjunkte Files, ∅ überall, kein G3, keine Lane), `wf_500a7a6c` lieferte **2× `approve` in Iteration 1** — der FOR-90-Reviewer bewies die „Iteration-1-Skeleton byte-unverändert"-AC per **Direkt-Ausführung und Byte-Diff der gerenderten Briefs**, nicht bloß über die Schema-Drift-Spec —, `--auto` landete beide direct-merged (#73/#74), und am eigenen Close feuerten beide Live-Gates: der Cleanup lief erstmals mit `.git`-zuletzt + bounded Retry (**beide `.git`-Dateien überlebten den erwarteten Sandbox-Fehlschlag; der reguläre `git worktree remove` funktionierte zum ersten Mal seit Bestehen der Orphan-Klasse**) und tolerierte erstmals das uniforme `--config` — Done-Reconcile 2× `merged` mit vollem `--acked`, Archiv plain-mv: **Tests 1798→1800, Backlog leer, 0 neue Funde**.

## 1. Was richtig gut funktioniert hat

- **Beide Live-Gates am eigenen Close bestanden.** Der Sandbox-Deny auf den Worktree-Pfaden bleibt der dokumentierte Normalfall — der Removal scheitert weiter, ehrlich als `erroredStillListed` klassifiziert. Der Unterschied ist die Hinterlassenschaft: statt halb-entfernter Leichen ohne `.git` (nur per `rm -rf` + `prune` sandbox-off zu räumen) stehen jetzt git-gültige Worktrees, die ein einziger regulärer `git worktree remove` beendet. Der bewusste Mensch-Schritt ist von Handarbeit auf einen sauberen Git-Befehl geschrumpft — genau die Reversibilität, die der W22-F1-Grill als fehlend markiert hatte.
- **Der Fix-Kreislauf des Tages schloss sich auf null.** W25 fand vier, W26 fixte sie und fand zwei, W27 fixte die zwei und fand **keinen**. Jeder Fund des Tages ist entweder gelandet oder bewusst als Playbook-Wissen abgelegt; zum ersten Mal seit Bestehen des Dogfoods endet ein Tag mit leerer Fund-Tabelle UND leerem Backlog.
- **Review-Evidenz auf neuem Niveau.** Die „prose-only, Skeleton byte-identisch"-AC von FOR-90 wurde nicht geglaubt, sondern bewiesen: der Reviewer rendrte `workerBrief()` alt gegen neu mit demselben synthetischen Issue-Objekt und diffte die Ausgaben — leer. Die Schema-Drift-Spec wurde korrekt als notwendig-aber-nicht-hinreichend eingeordnet.
- **Die Templates tragen.** Iteration-1-Driver und (ungenutzt, aber bereit) das Iter-2-Template aus W26 machten die Kompo-Zeit zur schnellsten bisher; die tracking-freie Checkout-Form und der Teardown-Schritt, die FOR-90 dokumentiert, sind ab dem nächsten Re-Dispatch die gebaute Realität statt Heldentat eines Workers.
- **Fünf Waves an einem Tag, ohne Qualitätsverlust am Ende:** die letzte Wave des Tages hatte die sauberste Bilanz.

## 2. Funde (nach Schwere)

**Keine.** Erste Wave ohne neuen Fund. Zur ehrlichen Einordnung, was *kein* Fund ist: das verbliebene `erroredStillListed` ×2 ist der dokumentierte, designte Normalfall unter Sandbox-Deny (die Klassifikation existiert genau dafür), und der Rest-Schritt `git worktree remove` sandbox-off ist die bewusste Mensch-Entscheidung aus dem W22-F1-Grill — beides Posture, kein Defekt.

## 3. Funde → Tickets

| Fund | Schwere | Status |
|---|---|---|
| — | — | **Keine Funde. Backlog leer.** |

## 4. Tagesschluss

Der 23. Juli 2026 endet bei **fünf Waves (W23–W27), vierzehn gelandeten Issues, siebzehn PRs (#58–#74), `main` von `d4649d7` nach `70af57b`, 1750→1800 Tests (+50)** — und einem System, das sich an einem einzigen Tag selbst umgebaut hat: morgens ohne CI, abends mit scharfem Ruleset-Gate, durch das jeder PR musste; beide Landing-Zweige mehrfach live gefeuert (inklusive zweier Retros, die sich per `armed` selbst mergten); die Pipeline shippt ihr Grill-Werkzeug selbst; Preflight liest Rulesets, Secrets haben ihren strukturellen Anker, der env-Block ersetzt das Prefix, der Cleanup hinterlässt Reversibles, beide Heuristik-Patterns sind gebunden, und der Re-Dispatch-Flow hat Teardown und tracking-freien Checkout im Playbook. **Der Fix-Kreislauf konvergierte auf null: keine offenen Funde, kein Backlog.** Die nächste Wave beginnt, wann immer der nächste echte Bedarf sie füllt — nicht, weil noch etwas abzuarbeiten wäre.

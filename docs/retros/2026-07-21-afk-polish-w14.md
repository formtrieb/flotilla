# flotilla — Retrospektive: Wave 14 „2026-07-21-afk-polish" (fünfzehnter Live-Lauf)

Wave: `2026-07-21-afk-polish` · Rows: **FOR-58, FOR-59, FOR-60** · Store: Linear (Formtrieb/`FOR`) · Repo: `github.com/formtrieb/flotilla` (öffentlich) · Anchor: `92c8b13` → `main` nach dem Close: `71e9811`.

Besonderheit dieses Laufs: **Same-Day-Loop und doppelte Selbstvalidierung.** Alle drei Rows waren Funde des W13-Close vom selben Tag — gefiled, geplant, dispatched, reviewed und gelandet binnen einer Session, ohne Protokollabstriche. Zwei der drei Fixes bewiesen sich an ihrer eigenen Wave: FOR-59 räumte beim eigenen Close die Worktrees weg, an denen der Vorgänger-Engine Minuten zuvor noch scheiterte (das FOR-51-Muster aus W11, drittes Vorkommen der Klasse), und FOR-60s repo-relative Driver-Form lief bereits als Compose-Praxis **dieser** Wave, bevor die Doku dazu gemergt war.

## 0. Ergebnis in einem Satz

Wave 14 lief `to-issues → wave-plan → wave-create → wave-start → wave-close --auto` in einem Zug — 3er-Fan-out (FOR-58 auf opus) → **3× `approve` in Iteration 1, 12/12 Agents, 0 Fehler, kein Re-Dispatch** → G3-STOP FOR-58 human-approved (additives `updated:boolean` auf `outcome:reused`) + Ein-Wave-Arm-Confirm → arm FOR-59 (sofort `merged`), Tail FOR-58→FOR-60 per `host-pr merge` (Merge-Trees clean wie prognostiziert) → `main 92c8b13 → 71e9811`, Gate **1627 Tests (+46) / tsc 0** → Done-Reconcile 3× `merged`, `--acked` maschinell → Archiv plain-mv, Claim-Ledger leer, Backlog danach **leer**, 0 Kern-Interventionen.

## 1. Was richtig gut funktioniert hat

- **FOR-59 validierte sich am eigenen Close.** Vor dem Merge scheiterte der Prä-Fix-Engine an den drei Wave-Worktrees mit `errors:3` (ENOTEMPTY-Klasse, 8. Repro); nach merge→pull lief derselbe Aufruf mit dem frisch gemergten Engine: **`removed:3, errors:0`** — Toplevel-Guard griff, Junk-Klassifikation lief auf Dir-Inhalt statt Eltern-Repo-Status, und die `worktree-wf_*`-Throwaway-Branches wurden gleich mitgelöscht. Ein Fix, der das Werkzeug seines eigenen Close repariert, ist die stärkste Live-Probe, die das System kennt (W11/FOR-51, W13/FOR-56-halb, jetzt vollständig).
- **FOR-60 war gelebte Praxis vor der eigenen Landung.** Der W14-Driver komponierte `WAVE_CLI` bereits repo-relativ mit cd-to-root-Scribes — die Wave probte also live, was ihre eigene Docs-Row festschreibt. Zusammen mit der tags zuvor gemergten getrackten Allowlist ist der Engine-CLI-Prompt-Pfad geschlossen; Worker-Terminatoren und Scribes liefen ohne Permission-Stall.
- **Die Wiring-Disclosure-Klausel (Convention 9, tags zuvor gelandet) feuerte im ersten Brief, der sie trug.** FOR-58s Worker nutzte sie mustergültig: neue Exporte (`findOpenPrRef`, `updateOpenPr`, Typen) mit Call-Site-Nachweis in-glob, und der bewusste **Nicht**-Export ins `index.ts`-Barrel explizit disclosed (Datei außerhalb der Globs). Kein Reviewer-Fund nötig — die W11/W12-Fundklasse ist vom Netz in den Brief gewandert.
- **FOR-58s Design-Entscheidungen waren additiv bis ins Detail.** `updated:boolean` als Property des bestehenden `reused`-Outcomes (kein neuer Enum-Wert → bestehende Konsumenten byte-unberührt); die dokumentierte number-Omission von `create` bleibt erhalten, obwohl der Reuse die Nummer intern kennt; Best-Effort-Posture (abgelehnter PATCH → `updated:false`, URL bleibt gepinnt, nie ein Throw). Der Ironie-Guard aus dem Brief bestätigte sich: der eigene PR wurde vom Prä-Fix-Engine als `created` geöffnet.
- **Merge-Tree-Prognosen erneut deckungsgleich.** Die eine Conflict-Zelle (FOR-58↔FOR-60 auf workflow-driver.md) blieb beim Merge konfliktfrei — beide Briefs hatten disjunkte Regionen vorgegeben (Termination-Statement vs. Compose-Konstanten), beide Reviewer prognostizierten die saubere Union.

## 2. Funde (nach Schwere)

### 🟢 KLEIN

**W14-F1 — Das `wave/*`-Löschsignal der neuen Branch-Hygiene feuert in der Praxis nie.** Die Hygiene löschte die `worktree-wf_*`-Throwaways korrekt und verweigerte die drei `wave/*`-Locals konservativ — richtig nach Spec, aber strukturell dauerhaft: Worker pushen ohne `-u` (kein konfigurierter Upstream → „upstream gone" kann nicht greifen), und Squash-Merges machen Tips nie contained. Das echte Löschsignal wäre **remote-ref-gone** (`git ls-remote` gegen den Branch-Namen nach dem Close-Delete) oder Dispatch-Log + merged-PR-Evidenz. Konservative Verweigerung ist der korrekte Default; das Signal-Upgrade ist ein sauberer Folge-Slice. **→ Ticket-Kandidat, gefiled als Follow-up.**

**W14-F2 — Coordinator-Shell-Footgun: `cd ..` aus `tools/wave` landet in `tools/`, nicht im Repo-Root.** Ein Close-Batch las danach Gate/Cleanup ins Leere (Exit-Codes statt Echos verrieten es); Wiederholung mit absoluten Pfaden lief sauber. Klasse W6-F1 (cwd-Reset-Batches mit lügenden Echos) — Merkregel bleibt: absolute Pfade in Close-Batches, Read-back statt Echo-Vertrauen.

**W14-F3 — Altlast-Sweep quantifiziert die Akkumulationsrate.** Vor der Wave: 48 `worktree-wf_*` + 9 `wave/*`-Locals aus 13 Waves manuell per `-D` geräumt (Merksatz: **squash ⇒ `-d` verweigert immer**). Ab dieser Wave übernimmt die FOR-59-Hygiene die Throwaways automatisch; die `wave/*`-Rate hängt am W14-F1-Follow-up.

**Wiederholungen:** Branch-Delete-Footgun ×6 (Checked-Step gelaufen, `ls-remote` = 0 Überlebende) · Stale-LSP-Flut ×~10 (kosmetisch; gemergter Tip real verifiziert 1627/tsc 0).

## 3. Funde → Tickets

| Fund | Schwere | Status |
|---|---|---|
| **W14-F1** — `wave/*`-Löschsignal: remote-ref-gone statt Upstream/Containment | 🟢 | **Ticket-Kandidat → gefiled** (Follow-up-Slice auf worktree-cleanup) |
| **W14-F2** — `cd ..`-Footgun in Close-Batches | 🟢 | Merkregel (absolute Pfade); kein Ticket |
| **W14-F3** — Altlast 57 Branches | 🟢 | Manuell geräumt; Rate ab jetzt durch FOR-59 gedeckelt |

## 4. Lauf-Metriken (grob)

- **Rows:** 3 (paralleler Fan-out; **Conflict-Map: 1 Zelle** FOR-58↔FOR-60, materialisierte sich nicht). **Verdicts Runde 1: 3× `approve`** — kein Re-Dispatch, Cap unberührt. **1 G3-STOP** (FOR-58, human-approved). **Agents:** **12/12, 0 Fehler**; ~32 min; ~0,95 Mio. Subagent-Tokens, 282 Tool-Calls.
- **Modelle:** 1× opus (FOR-58), 2× sonnet, Scribes haiku.
- **Landing (`--auto`):** arm FOR-59 → sofort `merged` (kein CI) · Tail FOR-58→FOR-60 → 2× `merged`. Alle 3 Branches remote gelöscht + verifiziert; lokal räumte die neue Hygiene die Throwaways selbst.
- **Tests:** 1581 → **1627** (+46: FOR-58 +13, FOR-59 +33; FOR-60 docs-only) · tsc 0 · auf dem gemergten Tip re-verifiziert. **ACs:** 14 über 3 Rows, alle met. **Sidecars:** 6, at-agent-return. **Done-Reconcile:** 3× `merged`, `--acked` maschinell. **Claim-Ledger:** leer. **Backlog nach Close:** leer. **Kern-Interventionen: 0.**

## 5. Meta-Reflexion

Der Lauf ist der bisher dichteste Beleg für die Loop-Geschwindigkeit, die das System erreichen soll: **Fund → Slice → Landung in unter einem Tag, bei vollem Protokoll** (DoR, Conflict-Map, universelle Reviews, G3, Advisory-Order, Done-Reconcile). Bemerkenswert ist weniger das Tempo als das, was dabei *nicht* passierte: kein Re-Dispatch, keine Intervention, keine Protokollabkürzung — die Geschwindigkeit kam aus der Reife der Maschinerie, nicht aus Weglassen.

Das zweite Muster ist die **Selbstvalidierungs-Kadenz**: Fixes, die die Close-Maschinerie selbst betreffen, bekommen ihr Live-Gate automatisch beim eigenen Close — inzwischen ein erwartbarer Rhythmus (FOR-51, FOR-56, FOR-59) statt eines glücklichen Zufalls. Die Konsequenz für die Planung: solche Slices sind *besonders gut* wave-geeignet, weil ihr Beweis gratis mitläuft.

**Watch-Items:** FOR-58s Update-on-Reuse wartet weiter auf seinen echten Live-Fall (ein Re-Dispatch, dessen Terminator den Verdict-Render auf einen Worker-geöffneten PR PATCHt). Die AFK-Kette misst sich an der Prompt-Zahl der nächsten Wave. **Vorwärts-Zeiger:** Der Backlog ist leer — der nächste Schritt ist das Mopla-Onboarding (wave-setup-Re-Run auf dem Server-Piloten mit beiden Preflights), wofür W13/W14 exakt die Adoption-Lücken geschlossen haben.
